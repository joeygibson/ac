#!/usr/bin/env bash
set -x

TAG=${GITHUB_REF#refs/*/}
ARCH=x86_64

if [[ "${OS}" == "macOS-latest" ]]; then
    VENDOR=apple
    OS=darwin
else
    VENDOR=unknown
    OS=linux
fi

mv target/debug/ac target/ac-${TAG}-${ARCH}-${VENDOR}-${OS}
chmod 755 target/ac-${TAG}-${ARCH}-${VENDOR}-${OS}
